export enum AppRoute {
  ProductPage = '/camera',
  Root = '/',
  NotFound = '/not-found',
}

export enum APIRoute {
  Cameras = '/cameras',
  Review = '/reviews',
}

export enum NameSpace {
  Cameras = 'CAMERAS',
}
